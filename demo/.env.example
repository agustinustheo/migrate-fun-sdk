# Solana Network Configuration
# Options: 'devnet' | 'mainnet-beta'
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Solana RPC URL
# For devnet: Use public RPC or get one from Helius/Alchemy
# For mainnet: Strongly recommended to use a private RPC (Helius, Alchemy, QuickNode, etc.)
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# Optional: Default Project ID for testing
# Get this from your migration project
NEXT_PUBLIC_DEFAULT_PROJECT_ID=mig1

# Optional: Use RPC proxy (recommended for production)
# Set to 'true' to use /api/rpc endpoint (you'll need to create it)
# This keeps your RPC API key secret on the server
NEXT_PUBLIC_USE_RPC_PROXY=false
